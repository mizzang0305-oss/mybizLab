// Temporary product-wide kill switch while public/merchant UX is being stabilized.
// Keep the MYBI implementation intact, but do not mount any companion UI until re-enabled intentionally.
export const ENABLE_MYBI_COMPANION = false;
